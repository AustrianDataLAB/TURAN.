apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "turandot.fullname" . }}-test-connection"
  labels:
    {{- include "turandot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "turandot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
